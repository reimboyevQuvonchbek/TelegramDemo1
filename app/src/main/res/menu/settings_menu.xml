<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">
    <item
        android:title=""
        android:icon="@drawable/ic_menu_popup"
        android:id="@+id/settings_menu1"
        app:showAsAction="ifRoom"
        >
        <menu>
            <item
                android:id="@+id/settings_menu_edit_name"
                android:title="Edit name"
                app:showAsAction="always"
                android:icon="@drawable/ic_menu_edit"/>
            <item
                android:id="@+id/settings_menu_exit"
                android:icon="@drawable/ic_menu_exit_icon"
                android:title="Long out"
                app:showAsAction="always" />
        </menu>
    </item>

</menu>